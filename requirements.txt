# requirements.txt actualizado y compatible
python-dateutil>=2.8.2,<3.0.0  # Rango que satisface todos los paquetes

# Paquetes principales (versiones probadas)
pandas==2.1.3  # Versión compatible con dateutil 2.8.2
hvac==1.1.0  # Alternativa moderna a vault
kubernetes==28.1.0
boto3==1.28.76

# Backend
Django==4.2.11
djangorestframework==3.14.0
