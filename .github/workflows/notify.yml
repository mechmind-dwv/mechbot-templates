name: Notify on Generation

on:
  workflow_run:
    workflows: ["Scheduled Generation"]
    types: [completed]

jobs:
  trigger-slack:
    runs-on: ubuntu-latest
    steps:
      - name: Notification placeholder
        run: echo "Scheduled Generation completed. Notifications would be sent here."
